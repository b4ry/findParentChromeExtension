const contentScript = require("../scripts/content-script");

describe("Content script", () => {
  test("clear test", () => {
    contentScript.clear();
  });
  test("findParent test", () => {
    contentScript.clear();
  });
});
